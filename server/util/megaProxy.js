const axios = require('axios')

const basePath = 'https://vmp-dev.i-tetris.com/';

module.exports = function (req, res, next) {
  console.log(req.url)
  debugger
  // axios(`${basePath}${req.url}`, {

  // })
}
